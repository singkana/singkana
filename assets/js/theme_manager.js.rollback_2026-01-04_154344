/* SingKANA Theme Manager – Dark LP Fixed */
(() => {
  // すべて無効化。ダーク固定。
  try {
    localStorage.removeItem("sk_theme");
  } catch {}
  document.documentElement.removeAttribute("class");
  document.body.removeAttribute("data-theme");
})();
